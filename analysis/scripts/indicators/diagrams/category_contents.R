geo_content <- list(site = c("site1", "site2"),
                    site_type = c("coastal", "RAMSAR"),
                    global = "global")

taxon_content <- list(species = c("species1", "species2"),
                      sp_type = c("herbivore", "herons"))

time_content <- list(time = "time")

indicator_content <- list(distribution = c("EOO", "AOO"),
                          abundance = c("abundance", "prop_total"))

process_lvl_type <- list(proc_lvl = c("data", "estimate", "change"))



